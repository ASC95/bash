# - Syntax:
#   - $ wc $: output the following in order: <line count> <word count> <byte count>
#   - $ wc -l $: only output line count
#   - $ wc -w $: only output word count
#   - $ wc -m $: only output character count (same as byte count if locale doesn't support multibyte characters)
#   - $ wc -c $: only output byte count
# - Remember that the newline is a character!
output() {

}