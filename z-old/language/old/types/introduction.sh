# https://www.tldp.org/LDP/abs/html/untyped.html

# Bash variables do not have any type. All Bash variables are characters strings.
# - If a variable contains only digits, certain other (arithmetic) operations are permitted
# Consequently, a variable may be 1) unset 2) set but an empty string (i.e. null or the null string) or 3) set with a value